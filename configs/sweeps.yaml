program: src/main.py
name: 0_ timesteps
method: grid
metric:
    name: val_acc
    goal: minimize
parameters:
    Encoder.num_train_timesteps:
        values: [1, 3, 5, 8, 10, 15, 20]

command:
    - ${env}
    - python
    - ${program}
    - ${args_no_hyphens}